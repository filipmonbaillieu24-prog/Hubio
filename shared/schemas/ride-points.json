{
  "$schema": "http://json-schema.org/draft-07/schema#",
  "title": "Zenith Route / Ride Points Schema",
  "description": "Validates the structure of the points array inside rides and routes tables.",
  "type": "array",
  "items": {
    "type": "object",
    "properties": {
      "lat": {
        "type": "number",
        "minimum": -90.0,
        "maximum": 90.0,
        "description": "Latitude coordinate of the point."
      },
      "lng": {
        "type": "number",
        "minimum": -180.0,
        "maximum": 180.0,
        "description": "Longitude coordinate of the point."
      },
      "ele": {
        "type": "number",
        "description": "Elevation in meters (elevation gain or height above sea level)."
      },
      "distance": {
        "type": "number",
        "minimum": 0.0,
        "description": "Cumulative distance from start of the ride in meters (typically used in routes)."
      },
      "time": {
        "type": "integer",
        "description": "Unix timestamp in milliseconds of the point recording (typically used in recorded rides)."
      }
    },
    "required": ["lat", "lng"]
  }
}
