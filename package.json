{
  "name": "zenith-ecosystem",
  "private": true,
  "version": "0.1.0",
  "workspaces": [
    "apps/*"
  ],
  "scripts": {
    "aero:dev": "npm run dev --workspace=apps/aero",
    "aero:build": "npm run build --workspace=apps/aero",
    "aero:tauri": "npm run tauri --workspace=apps/aero"
  }
}
